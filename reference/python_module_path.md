# Chemin vers le module Python d'inference MAESTRO

Retourne le chemin du repertoire contenant `maestro_inference.py`,
installe avec le package dans `inst/python/`.

## Usage

``` r
python_module_path()
```

## Value

Chemin du repertoire Python (character)
