% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rapport.R
\name{generer_rapport_segmentation}
\alias{generer_rapport_segmentation}
\title{Generer un rapport de segmentation MAESTRO (HTML ou PDF)}
\usage{
generer_rapport_segmentation(
  aoi_path,
  backbone_path,
  decoder_path = "segmenter_ndp0_best.pt",
  output_dir = "outputs",
  format = c("html", "pdf"),
  output_file = NULL,
  millesime_ortho = NULL,
  millesime_irc = NULL,
  dem_channels = c("SLOPE", "TWI"),
  use_s2 = FALSE,
  use_s1 = FALSE,
  date_sentinel = NULL,
  gpu = FALSE,
  run_segmentation = TRUE,
  use_flair = FALSE,
  model_flair = "IGNF/FLAIR-INC_rgbi_15cl_resnet34-unet",
  open = interactive()
)
}
\arguments{
\item{aoi_path}{Chemin vers le fichier GeoPackage de l'AOI.}

\item{backbone_path}{Chemin vers le checkpoint MAESTRO (.ckpt).}

\item{decoder_path}{Chemin vers le decodeur de segmentation (.pt).}

\item{output_dir}{Repertoire de sortie (defaut \code{"outputs"}).}

\item{format}{Format de sortie : \code{"html"} (par defaut) ou \code{"pdf"}.}

\item{output_file}{Nom du fichier de sortie (optionnel).}

\item{millesime_ortho}{Millesime de l'ortho RVB (\code{NULL} = plus recent).}

\item{millesime_irc}{Millesime de l'ortho IRC (\code{NULL} = plus recent).}

\item{dem_channels}{Vecteur de 2 canaux DEM parmi \code{"DSM"}, \code{"DTM"},
\code{"SLOPE"}, \code{"ASPECT"}, \code{"TPI"}, \code{"TWI"} (defaut \code{c("SLOPE", "TWI")}).}

\item{use_s2}{Logical. Inclure Sentinel-2 ? (defaut \code{FALSE}).}

\item{use_s1}{Logical. Inclure Sentinel-1 ? (defaut \code{FALSE}).}

\item{date_sentinel}{Date cible pour Sentinel (\code{NULL}).}

\item{gpu}{Logical. Utiliser le GPU ? (defaut \code{FALSE}).}

\item{run_segmentation}{Logical. Executer la segmentation ? Si \code{FALSE},
charge un resultat existant depuis \code{output_dir} (defaut \code{TRUE}).}

\item{use_flair}{Logical. Appliquer la contrainte FLAIR feuillus/resineux
apres la segmentation ? (defaut \code{FALSE}).}

\item{model_flair}{Identifiant du modele FLAIR HuggingFace
(defaut \code{"IGNF/FLAIR-INC_rgbi_15cl_resnet34-unet"}).}

\item{open}{Logical. Ouvrir le rapport apres generation ?
(defaut \code{TRUE} en session interactive).}
}
\value{
Le chemin du fichier rapport genere (invisiblement).
}
\description{
Compile le template Rmd de segmentation pour produire un rapport complet :
donnees d'entree (RGBI, DEM avec SLOPE/TWI/TPI/ASPECT, Sentinel),
indices spectraux (NDVI), carte de segmentation NDP0 a 0.2m,
distribution des essences, et analyses topographiques croisees.
}
\examples{
\dontrun{
# Rapport HTML de segmentation
generer_rapport_segmentation(
  aoi_path      = "data/aoi.gpkg",
  backbone_path = modele$weights,
  decoder_path  = "segmenter_ndp0_best.pt"
)

# Rapport PDF avec DEM DSM+DTM et Sentinel-2
generer_rapport_segmentation(
  aoi_path      = "data/aoi.gpkg",
  backbone_path = modele$weights,
  decoder_path  = "segmenter_ndp0_best.pt",
  format        = "pdf",
  dem_channels  = c("DSM", "DTM"),
  use_s2        = TRUE
)

# Rapport depuis resultats existants (sans re-executer)
generer_rapport_segmentation(
  aoi_path         = "data/aoi.gpkg",
  backbone_path    = NULL,
  decoder_path     = "segmenter_ndp0_best.pt",
  run_segmentation = FALSE
)
}
}
