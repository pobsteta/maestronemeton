% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rapport.R
\name{generer_rapport}
\alias{generer_rapport}
\title{Generer un rapport HTML ou PDF du pipeline MAESTRO}
\usage{
generer_rapport(
  aoi_path,
  output_dir = "outputs",
  format = c("html", "pdf"),
  output_file = NULL,
  millesime = NULL,
  inference = FALSE,
  gpu = FALSE,
  model_id = "IGNF/MAESTRO_FLAIR-HUB_base",
  use_s2 = FALSE,
  use_s1 = FALSE,
  date_sentinel = NULL,
  open = interactive()
)
}
\arguments{
\item{aoi_path}{Chemin vers le fichier GeoPackage de l'AOI.}

\item{output_dir}{Repertoire de sortie pour les resultats et le rapport
(par defaut \code{"outputs"}).}

\item{format}{Format de sortie : \code{"html"} (par defaut) ou \code{"pdf"}.
Le format PDF necessite une installation LaTeX (voir
\code{\link[tinytex:install_tinytex]{tinytex::install_tinytex()}}).}

\item{output_file}{Nom du fichier de sortie (optionnel). Par defaut
\code{"rapport_pipeline_aoi.html"} ou \code{"rapport_pipeline_aoi.pdf"}.}

\item{millesime}{Millesime des orthophotos IGN (\code{NULL} = le plus recent).}

\item{inference}{Logical. Lancer l'inference MAESTRO ? (defaut \code{FALSE}).}

\item{gpu}{Logical. Utiliser le GPU pour l'inference ? (defaut \code{FALSE}).}

\item{model_id}{Identifiant du modele sur Hugging Face
(defaut \code{"IGNF/MAESTRO_FLAIR-HUB_base"}).}

\item{use_s2}{Logical. Telecharger et integrer les donnees Sentinel-2 ?
(defaut \code{FALSE}).}

\item{use_s1}{Logical. Telecharger et integrer les donnees Sentinel-1 ?
(defaut \code{FALSE}).}

\item{date_sentinel}{Date cible pour les donnees Sentinel (format
\code{"YYYY-MM-DD"} ou \code{NULL}).}

\item{open}{Logical. Ouvrir le rapport dans le navigateur apres generation ?
(defaut \code{TRUE} en session interactive).}
}
\value{
Le chemin du fichier rapport genere (invisiblement).
}
\description{
Compile le template Rmd fourni avec le package pour produire un rapport
complet du pipeline : telechargement des donnees, combinaison, creation
des patches, indices spectraux et optionnellement inference.
}
\examples{
\dontrun{
# Rapport HTML simple
generer_rapport("data/aoi.gpkg")

# Rapport PDF avec inference
generer_rapport("data/aoi.gpkg", format = "pdf", inference = TRUE)

# Rapport complet avec Sentinel-2 et inference GPU
generer_rapport(
  aoi_path   = "data/aoi.gpkg",
  format     = "html",
  inference  = TRUE,
  gpu        = TRUE,
  use_s2     = TRUE,
  date_sentinel = "2024-06-15"
)
}
}
