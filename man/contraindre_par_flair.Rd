% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flair_contrainte.R
\name{contraindre_par_flair}
\alias{contraindre_par_flair}
\title{Contraindre la segmentation MAESTRO par la classification FLAIR}
\usage{
contraindre_par_flair(
  raster_seg,
  raster_flair,
  garder_mixte = TRUE,
  garder_ligneux = TRUE
)
}
\arguments{
\item{raster_seg}{SpatRaster de segmentation MAESTRO (classes NDP0 0-9)}

\item{raster_flair}{SpatRaster de classification FLAIR (classes CoSIA 1-19)}

\item{garder_mixte}{Logical. Garder la prediction MAESTRO pour les pixels
"Mixte conifere+feuillu" (classe 17) ? Si \code{FALSE}, force en feuillus
divers (defaut \code{TRUE}).}

\item{garder_ligneux}{Logical. Traiter les pixels "Ligneux" (classe 18)
comme foret ? (defaut \code{TRUE}).}
}
\value{
Liste avec :
\itemize{
\item \code{raster} : SpatRaster contraint
\item \code{stats} : data.frame avec les corrections appliquees
}
}
\description{
Post-traitement qui utilise la carte FLAIR (CoSIA 19 classes) pour :
\enumerate{
\item Forcer les pixels non-foret a la classe NDP0 = 9
\item Contraindre les pixels "Conifere" FLAIR aux essences resineuses
\item Contraindre les pixels "Feuillu" FLAIR aux essences feuillues
\item Gerer les pixels "Mixte" en gardant la prediction MAESTRO
}
}
\examples{
\dontrun{
# Lancer FLAIR puis contraindre
flair_result <- flair_pipeline("data/aoi.gpkg", output_dir = "outputs")
raster_flair <- terra::rast("outputs/occupation_sol.tif")

result <- contraindre_par_flair(raster_seg, raster_flair)
terra::plot(result$raster)
}
}
