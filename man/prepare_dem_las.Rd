% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lidar.R
\name{prepare_dem_las}
\alias{prepare_dem_las}
\title{Pipeline LiDAR \code{lasR} pour deriver DSM + DTM d'une AOI}
\usage{
prepare_dem_las(
  aoi,
  las_files,
  output_dir,
  rgbi = NULL,
  buffer_m = 20,
  ncores = 2L,
  keep_tiles = FALSE
)
}
\arguments{
\item{aoi}{sf object en Lambert-93.}

\item{las_files}{Vecteur character de chemins vers des fichiers LAZ/LAS.}

\item{output_dir}{Repertoire de sortie (ecrit \code{dem_2bands.tif} et les
tuiles intermediaires \code{lasr_dtm_*.tif}, \code{lasr_dsm_*.tif}).}

\item{rgbi}{SpatRaster aerial de reference pour le reechantillonnage a
0,2 m (NULL = garde la resolution native 1 m).}

\item{buffer_m}{Buffer (m) applique au LAScatalog pour les effets de
bord (defaut : 20, valeur recommandee dans DEV_PLAN.md S3.3).}

\item{ncores}{Nombre de coeurs paralleles (defaut : 2).}

\item{keep_tiles}{Si TRUE, conserve les rasters par tuile dans
\code{output_dir}. Sinon (defaut), les supprime apres mosaiquage.}
}
\value{
Liste avec
\itemize{
\item \code{dem} : SpatRaster 2 bandes nommees \code{DSM}, \code{DTM}
\item \code{dem_path} : chemin du GeoTIFF ecrit
\item \code{dsm_source} : \code{"lasR"}
\item \code{lidar_hd_coverage_pct} : NA (la mesure de couverture WMS n'a pas
de sens en mode LAS ou les points sont par construction LiDAR)
}
}
\description{
Execute un pipeline \code{lasR} sur les nuages LAZ fournis : triangulation TIN
sur les points classes sol (ASPRS 2) pour le DTM, sur les premiers retours
(return_number == 1) pour le DSM, rasterisation a 1 m avec un buffer de
20 m sur le \code{LAScatalog} pour gerer les effets de bord. Les rasters par
tuile sont ensuite mosaiques, croppes sur l'AOI puis ree-chantillonnes
sur la grille de la modalite \code{aerial} (resolution 0,2 m typiquement).
}
\details{
Cette fonction est la voie \code{source = "las"} de \code{\link[=prepare_dem]{prepare_dem()}} (cf. ticket
P2-02 du \code{DEV_PLAN.md}). Le pattern lasR est repris du tutoriel
\code{pobsteta/nemeton} \code{inst/tutorials/07-lidar-advanced} (cf. DEV_PLAN.md S3.3).

Le telechargement automatique des tuiles LAZ depuis l'IGN
(catalogue cartes.gouv.fr / S3) est hors scope de cette fonction : les
chemins sont a fournir explicitement via \code{las_files}.
}
\keyword{internal}
