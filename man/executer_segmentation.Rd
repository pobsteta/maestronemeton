% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{executer_segmentation}
\alias{executer_segmentation}
\title{Executer la segmentation dense MAESTRO sur toute l'AOI}
\usage{
executer_segmentation(
  segmenter,
  modalites,
  aoi,
  output_dir = "outputs",
  patch_size = 250L,
  resolution = 0.2,
  overlap_m = 10,
  gpu = FALSE,
  batch_size = 4L
)
}
\arguments{
\item{segmenter}{Modele MAESTROSegmenter}

\item{modalites}{Liste nommee de SpatRasters complets pour l'AOI
(ex: \code{list(aerial=rgbi, dem=dem)})}

\item{aoi}{sf object en Lambert-93}

\item{output_dir}{Repertoire de sortie}

\item{patch_size}{Taille des patches en pixels (defaut: 250)}

\item{resolution}{Resolution en metres (defaut: 0.2)}

\item{overlap_m}{Recouvrement entre patches en metres (defaut: 10)}

\item{gpu}{Utiliser CUDA}

\item{batch_size}{Taille des batchs (defaut: 4)}
}
\value{
SpatRaster mono-bande avec les codes NDP0 a 0.2m
}
\description{
Decoupe l'AOI en patches de 50m, execute la segmentation par patch
via le backbone MAESTRO + decodeur, puis reassemble les predictions
en une carte continue a 0.2m de resolution.
}
\details{
Les patches se chevauchent de 10m (overlap) et les zones de recouvrement
sont resolues par vote de la classe avec la probabilite maximale.
}
