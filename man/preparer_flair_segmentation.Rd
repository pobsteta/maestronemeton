% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation.R
\name{preparer_flair_segmentation}
\alias{preparer_flair_segmentation}
\title{Preparer les patches FLAIR-HUB pour l'entrainement du decodeur}
\usage{
preparer_flair_segmentation(
  flair_dir = "data/flair_hub",
  output_dir = "data/segmentation",
  modalites = c("aerial", "dem"),
  val_ratio = 0.15,
  min_forest_pct = 10,
  domaines = NULL,
  max_patches = NULL
)
}
\arguments{
\item{flair_dir}{Repertoire racine FLAIR-HUB contenant aerial/, dem/,
labels_ndp0/ (issu de \code{\link[=labelliser_flair_bdforet]{labelliser_flair_bdforet()}})}

\item{output_dir}{Repertoire de sortie pour les patches reorganises}

\item{modalites}{Vecteur des modalites a inclure (defaut: \code{c("aerial", "dem")})}

\item{val_ratio}{Proportion de patches pour la validation (defaut: 0.15)}

\item{min_forest_pct}{Pourcentage minimum de foret pour garder un patch
(defaut: 10)}

\item{domaines}{Vecteur de domaines a utiliser (\code{NULL} = tous)}

\item{max_patches}{Nombre maximum de patches a utiliser (\code{NULL} = tous)}
}
\value{
Liste avec n_train, n_val, n_skipped
}
\description{
Organise les patches FLAIR-HUB existants (deja labellises avec
\code{\link[=labelliser_flair_bdforet]{labelliser_flair_bdforet()}}) en structure train/val attendue par
\code{train_segmentation.py}. Les patches avec moins de \code{min_forest_pct}\%
de foret sont exclus.
}
\details{
Structure de sortie :

\if{html}{\out{<div class="sourceCode">}}\preformatted{output_dir/
  train/
    aerial/   patch_00001.tif, ...
    dem/      patch_00001.tif, ...
    labels/   patch_00001.tif, ...
  val/
    aerial/   ...
    dem/      ...
    labels/   ...
}\if{html}{\out{</div>}}
}
