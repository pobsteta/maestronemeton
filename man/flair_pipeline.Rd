% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flair_pipeline.R
\name{flair_pipeline}
\alias{flair_pipeline}
\title{Executer le pipeline FLAIR d'occupation du sol}
\usage{
flair_pipeline(
  aoi_path = "data/aoi.gpkg",
  output_dir = "outputs",
  model_id = "IGNF/FLAIR-INC_rgbi_15cl_resnet34-unet",
  encoder = "resnet34",
  decoder = "unet",
  n_classes = 19L,
  dem_channels = NULL,
  millesime_ortho = NULL,
  millesime_irc = NULL,
  patch_size = 512L,
  overlap = 128L,
  gpu = FALSE,
  token = NULL
)
}
\arguments{
\item{aoi_path}{Chemin vers le fichier GeoPackage de la zone d'interet}

\item{output_dir}{Repertoire de sortie (defaut: \code{"outputs"})}

\item{model_id}{Identifiant du modele Hugging Face
(defaut: \code{"IGNF/FLAIR-INC_rgbi_15cl_resnet34-unet"})}

\item{encoder}{Architecture encodeur (defaut: \code{"resnet34"})}

\item{decoder}{Architecture decodeur (defaut: \code{"unet"})}

\item{n_classes}{Nombre de classes du modele (defaut: 19, les checkpoints
FLAIR-INC ont 19 canaux de sortie meme pour 15 classes actives)}

\item{dem_channels}{Canaux DEM a ajouter. \code{NULL} = pas de DEM (RGBI seul, 4 bandes).
Vecteur de noms parmi \code{c("DSM", "DTM", "SLOPE", "ASPECT", "TPI", "TWI")}.
Ex: \code{c("SLOPE", "TWI")} = 6 bandes (RGBI + pente + TWI).
Ex: \code{"DTM"} = 5 bandes (RGBI + DTM classique).}

\item{millesime_ortho}{Millesime de l'ortho RVB (\code{NULL} = plus recent)}

\item{millesime_irc}{Millesime de l'ortho IRC (\code{NULL} = plus recent)}

\item{patch_size}{Taille des patches en pixels (defaut: 512)}

\item{overlap}{Recouvrement entre patches (defaut: 128)}

\item{gpu}{Utiliser le GPU CUDA (defaut: FALSE)}

\item{token}{Token Hugging Face (optionnel)}
}
\value{
Liste invisible avec \code{raster} (SpatRaster) et \code{stats} (data.frame)
}
\description{
Pipeline de bout en bout : charge l'AOI, telecharge les donnees IGN
(ortho RVB, IRC, DEM optionnel), combine les bandes, telecharge le modele
FLAIR pre-entraine, execute la segmentation semantique pixel-a-pixel
avec blending Hann, et exporte les resultats.
}
\details{
Les modeles FLAIR produisent des cartes de classification par pixel,
contrairement a MAESTRO qui classifie par patch.
}
\examples{
\dontrun{
# Classification basique RGBI (4 bandes)
flair_pipeline("data/aoi.gpkg")

# Avec pente + TWI (6 bandes, optimal foret)
flair_pipeline("data/aoi.gpkg",
                dem_channels = c("SLOPE", "TWI"),
                model_id = "IGNF/FLAIR-INC_rgbie_15cl_resnet34-unet")

# Avec DTM classique (5 bandes)
flair_pipeline("data/aoi.gpkg",
                dem_channels = "DTM",
                model_id = "IGNF/FLAIR-INC_rgbie_15cl_resnet34-unet")
}
}
