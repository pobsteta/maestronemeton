% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_ign.R
\name{prepare_dem}
\alias{prepare_dem}
\title{Preparer la modalite \code{dem} MAESTRO pour une AOI (DSM + DTM)}
\usage{
prepare_dem(
  aoi,
  output_dir,
  rgbi = NULL,
  source = c("wms", "las"),
  coverage_threshold = 10,
  allow_dtm_only_fallback = TRUE,
  las_files = NULL,
  ncores = 2L
)
}
\arguments{
\item{aoi}{sf object en Lambert-93.}

\item{output_dir}{Repertoire de sortie (ecrit \code{dem_2bands.tif}).}

\item{rgbi}{SpatRaster aerial de reference pour le reechantillonnage a
0,2 m (NULL = garde la resolution native 1 m).}

\item{source}{\code{"wms"} (defaut) pour Geoplateforme IGN. \code{"las"} pour
derivation locale via lasR a partir de \code{las_files}.}

\item{coverage_threshold}{Pourcentage minimum de pixels LiDAR HD valides
pour considerer la couverture suffisante en mode WMS (defaut : 10).}

\item{allow_dtm_only_fallback}{Mode WMS uniquement. Si TRUE et couverture
insuffisante, utilise DSM = DTM (warning) ; sinon retourne NULL.}

\item{las_files}{Mode \code{"las"} uniquement : vecteur de chemins vers les
tuiles LAZ/LAS a traiter (1 km x 1 km IGN typiquement).}

\item{ncores}{Mode \code{"las"} uniquement : nombre de coeurs paralleles
pour le pipeline lasR (defaut : 2).}
}
\value{
Liste avec
\itemize{
\item \code{dem} : SpatRaster 2 bandes nommees \code{DSM}, \code{DTM}
\item \code{dem_path} : chemin du GeoTIFF ecrit
\item \code{dsm_source} : \code{"lidar_hd"} | \code{"rge_alti_fallback"} | \code{"lasR"} | \code{"cache"}
\item \code{lidar_hd_coverage_pct} : couverture LiDAR HD mesuree (mode WMS) ou
NA (mode LAS)
Renvoie NULL si le DTM RGE ALTI ne peut pas etre telecharge (mode WMS),
ou si le LiDAR HD est insuffisant et \code{allow_dtm_only_fallback = FALSE}.
}
}
\description{
Construit le raster 2 bandes attendu par la modalite \code{dem} du modele
MAESTRO : \verb{(DSM, DTM)} dans cet ordre. Deux sources sont supportees :
}
\details{
\strong{\code{source = "wms"}} (defaut) : DTM depuis le RGE ALTI 1 m (couverture
nationale, couche WMS \code{ELEVATION.ELEVATIONGRIDCOVERAGE.HIGHRES}),
DSM depuis le LiDAR HD IGN (couverture partielle en cours de completion
d'ici 2026, couche \code{IGNF_LIDAR-HD_MNS_ELEVATION...}). Quand le LiDAR HD
ne couvre pas l'AOI (couverture < \code{coverage_threshold}), deux strategies
selon \code{allow_dtm_only_fallback} :
\itemize{
\item \code{TRUE} (defaut) : duplique le DTM comme DSM avec un warning explicite.
Le CHM (DSM - DTM) sera plat, ce qui degrade le signal pour MAESTRO.
A reserver aux mode degrade ou tests d'integration.
\item \code{FALSE} : retourne \code{NULL} pour que l'appelant retire la modalite \code{dem}
du pipeline et evite d'injecter des donnees corrompues dans le modele.
}

\strong{\code{source = "las"}} : derivation directe via \link[lasR:lasR-package]{lasR}
a partir des nuages LAZ fournis dans \code{las_files}. Pipeline TIN sol +
premiers retours, buffer 20 m sur le \code{LAScatalog}, mosaiquage et crop
sur l'AOI, resample sur la grille aerial. Le telechargement automatique
des tuiles LAZ depuis l'IGN n'est pas (encore) couvert : les chemins
sont a fournir explicitement (cf. catalogue cartes.gouv.fr).
}
